server:
  port: 8085

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8090/eureka
  instance:
    hostname: localhost

spring:
  cloud:
    gateway:
      routes:
        - id: ms-estudiante-service
          uri: lb://ms-estudiante-service
          predicates:
            - Path=/estudiantes/**
        - id: ms-estudiante-service
          uri: lb://ms-estudiante-service
          predicates:
            - Path=/apoderados/**
        - id: ms-insidencia-service
          uri: lb://ms-insidencia-service
          predicates:
            - Path=/insidencias/**    -

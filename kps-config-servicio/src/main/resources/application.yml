server:
  port: 7070
spring:
  application:
    name: kps-config-servicio
  cloud:
    config:
      server:
        git:
          uri: https://github.com/keinraf/Microservicios-KEVINRAF-MANUAL.git
          searchPaths: config-data
          default-label: MainKevin
  security:
    user:
      name: kevinraf
      password: root